package com.kn.stringPrograms;

public class ReadCharacter {

		public char charAt(String string,int index) {
			char ch=string.charAt(index);
			return ch;
		}
		public char toCharArray(String string,int index) {
			char []ch=string.toCharArray();
			return ch[index];
		}
}
